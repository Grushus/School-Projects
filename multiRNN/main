import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
import seaborn as sns
import tensorflow as tf
from sklearn.preprocessing import MinMaxScaler
from tensorflow.keras.preprocessing.sequence import TimeseriesGenerator
from tensorflow.keras.models import Sequential
from tensorflow.keras.layers import Dense,LSTM
from tensorflow.keras.callbacks import EarlyStopping
from MultiRNN import code

# object example
'''
example = MultiRNN(csv="path to file",
                   test_size=,
                   length=,
                   LSTM_units=,
                   activation="",
                   optimizer="",
                   batch_size=,
                   epochs=,
                   drop_columns=[""],
                   date_cutoff="",
                   save_losses=False,
                   save_model=False)
'''

#example.predict()
#example.plot_predict_against_test_dataset_per_column()
#example.plot_loss_val_loss_per_column()
#example.forecast_per_column()
#example.plot_forecast_per_column_against_dataset()
